interface PageProps {
    searchParams: {
        [key: string]: string | string[] | undefined
    }
}

const Page = ({ searchPatams }: PageProps) => {

    const query

    return (
        <p>Search</p>
    )
}

export default Page